// Twitter card image — identical to the OG card; Next.js only attaches
// twitter:image from a twitter-image file, so we re-export.
export { default, alt, size, contentType } from "./opengraph-image";
